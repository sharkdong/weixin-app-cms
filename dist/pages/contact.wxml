
  <map id="map" longitude="{{siteInfo.longitude}}" latitude="{{siteInfo.latitude}}" scale="14" controls="{{controls}}" bindcontroltap="controltap" markers="{{markers}}" bindmarkertap="markertap" polyline="{{polyline}}" bindregionchange="regionchange" show-location="show-location" style="width: 100%; height: 300px;">
  <cover-view class="logo">
  <cover-image class="logo-img" src="{{linkUpload}}{{siteInfo.siteLogo}}"/>
  </cover-view>
  </map>
  <view class=".card-content">
  <rich-text class="card-desc" nodes="{{siteInfo.recordInformation}}"/>
  </view>